name: Terraform

on:
  push:
    branches:
      - main
      - dev
    paths:
      - "terraform/**"
  pull_request:
    paths:
      - "terraform/**"

env:
  TF_VERSION: "1.13.5"

jobs:
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    timeout-minutes: 30 # Set overall job timeout

    strategy:
      matrix:
        environment: [dev, prod]
      max-parallel: 1 # Prevent concurrent runs that could cause state locks
      fail-fast: false # Continue with other environments if one fails

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3.1.2
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Verify Terraform Installation
        run: |
          terraform version
          which terraform

      - name: Google Auth
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Terraform Format Check
        run: |
          echo "Checking Terraform format across all directories..."
          if ! terraform fmt -check -recursive .; then
            echo "❌ Terraform files are not formatted correctly!"
            echo "💡 Run 'make fmt' or 'terraform fmt -recursive .' to fix"
            exit 1
          fi
          echo "✅ All Terraform files are properly formatted"

      - name: Terraform Init
        working-directory: terraform/environments/${{ matrix.environment }}
        run: terraform init -lock-timeout=10m
        continue-on-error: false

      - name: Terraform Validate
        working-directory: terraform/environments/${{ matrix.environment }}
        run: terraform validate && terraform force-unlock 1762635625528616

      - name: Terraform Plan with Retry
        working-directory: terraform/environments/${{ matrix.environment }}
        timeout-minutes: 20 # Set step timeout
        run: |
          # Function to check and clean stale locks
          cleanup_stale_locks() {
            echo "🔍 Checking for existing locks..."
            # Get lock info and clean up if needed
            if terraform show -json 2>/dev/null | grep -q "lock_id"; then
              lock_id=$(terraform show -json 2>/dev/null | jq -r '.lock_id // empty' 2>/dev/null || echo "")
              if [ ! -z "$lock_id" ]; then
                echo "⚠️  Found existing lock: $lock_id"
                echo "🧹 Attempting to remove stale lock..."
                terraform force-unlock -force "$lock_id" || true
                sleep 5
              fi
            fi
          }

          # Function to attempt terraform plan with retries
          attempt_plan() {
            local max_attempts=3
            local attempt=1

            # Clean up any potential stale locks first
            cleanup_stale_locks

            while [ $attempt -le $max_attempts ]; do
              echo "Attempt $attempt of $max_attempts..."
              echo "Environment: ${{ matrix.environment }}"
              echo "Timestamp: $(date)"

              # Use a longer lock timeout and add refresh
              if terraform plan -out=tfplan -lock-timeout=10m -refresh=true -parallelism=4; then
                echo "✅ Terraform plan successful"
                return 0
              else
                echo "❌ Terraform plan failed on attempt $attempt"

                if [ $attempt -eq $max_attempts ]; then
                  echo "🔍 Final attempt to clean locks..."
                  cleanup_stale_locks
                  echo "💥 All attempts failed"
                  return 1
                fi

                echo "⏳ Waiting 60 seconds before retry..."
                sleep 60

                # Clean up before retry
                cleanup_stale_locks

                ((attempt++))
              fi
            done
          }

          attempt_plan

      - name: Upload Plan
        uses: actions/upload-artifact@v5.0.0
        with:
          name: tfplan-${{ matrix.environment }}
          path: terraform/environments/${{ matrix.environment }}/tfplan

      - name: Cleanup on Failure
        if: always() # Run cleanup even on success to prevent stale locks
        working-directory: terraform/environments/${{ matrix.environment }}
        run: |
          echo "🧹 Post-execution cleanup..."

          # List any terraform processes
          echo "Active terraform processes:"
          ps aux | grep terraform || echo "No terraform processes found"

          # Clean up any remaining locks
          echo "Checking for locks to clean up..."
          if terraform show -json 2>/dev/null | grep -q "lock_id"; then
            lock_id=$(terraform show -json 2>/dev/null | jq -r '.lock_id // empty' 2>/dev/null || echo "")
            if [ ! -z "$lock_id" ]; then
              echo "Found lock to clean: $lock_id"
              terraform force-unlock -force "$lock_id" 2>/dev/null || true
            fi
          fi

          echo "✅ Cleanup completed"

  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: terraform-plan
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    timeout-minutes: 45 # Set overall job timeout

    strategy:
      matrix:
        environment: [prod]
      max-parallel: 1 # Deploy one environment at a time
      fail-fast: false

    environment:
      name: ${{ matrix.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3.1.2
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Verify Terraform Installation
        run: |
          terraform version
          which terraform

      - name: Google Auth
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Terraform Init
        working-directory: terraform/environments/${{ matrix.environment }}
        run: terraform init -lock-timeout=10m

      - name: Download Plan
        uses: actions/download-artifact@v4
        with:
          name: tfplan-${{ matrix.environment }}
          path: terraform/environments/${{ matrix.environment }}

      - name: Terraform Apply with Lock Handling
        working-directory: terraform/environments/${{ matrix.environment }}
        run: |
          # Apply with extended lock timeout
          terraform apply -auto-approve -lock-timeout=15m tfplan

      - name: Cleanup on Failure
        if: failure()
        working-directory: terraform/environments/${{ matrix.environment }}
        run: |
          echo "🧹 Cleaning up potential state locks..."
          terraform force-unlock -force $(terraform show -json 2>/dev/null | jq -r '.lock_id // empty') 2>/dev/null || true
          echo "✅ Cleanup completed"
