repos:
  - repo: local
    hooks:
      - id: terraform-fmt
        name: Terraform fmt
        entry: terraform fmt
        args: ["-recursive", "-check"]
        language: system
        files: \.tf$
        pass_filenames: false
